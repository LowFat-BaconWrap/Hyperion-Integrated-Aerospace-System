# Flight Model

The simulation models the launch as a six-state system:

State vector:

[x, y, vx, vy, θ, ω]

x = horizontal position
y = altitude
vx = horizontal velocity
vy = vertical velocity
θ = rocket orientation (Pitch)
ω = angular velocity

Relevant equations:
